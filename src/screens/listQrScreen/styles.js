
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
  },
  scrollContent: {
    alignItems: 'center',
    display: 'flex',
    flexGrow: 1
  }
});

export default styles;
